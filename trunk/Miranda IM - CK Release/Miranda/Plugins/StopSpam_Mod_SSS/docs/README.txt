* Build Dependencies
    1. boost (http://www.boost.org/)
    2. c++ compiller (because of Miranda IM have compiled with MSVC, only msvc compiled builds working fine (i have no success with mingw, please report your result :) ))
* Runtime Dependencies
    1. Miranda IM at least 0.8 (custom builds should be compiled fine against 0.6, 0.7 miranda api)
